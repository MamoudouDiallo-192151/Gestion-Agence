{% extends "base.html.twig" %}
{% block title %}Agence_Diallo
{% endblock %}
{% block body %}
	<div class="jumbotron text-center">
		<h1>Agence Immobilière Diallo Mamoudou</h1>
	</div>
	<div class="container">
		<h2>Les derniers biens</h2>
		<div class="row flex">
			{%  for property in properties%}
				<div class="col-3">
					<div class="card">
						<div class="card-body">
							<h5 class="card-title">
								<a href="{{path('property_show',{id:property.id, slug: property.slug})}}">{{property.title}}</a>
							</h5>
							<p>
								<div class="card-text">
									{{property.city}}
									({{property.postalCode}})</div>
							</p>
							<div class="text-primary" style="font-weight: bold;font-size: 2rem;">{{property.getFormattedPrice}}
								D</div>
						</div>
					</div>
				</div>
			{% endfor %}

		</div>
	</div>

{% endblock %}
