{% extends "base.html.twig" %}
{% form_theme formSearch 'bootstrap_5_layout.html.twig' %}

{% block title %}Voir tous nos biens
{% endblock %}
{% block body %}
	<div class="container-fluid bg-light p-5">
		{{form_start(formSearch)}}
		<div class="row">
			<div class="col">{{form_row(formSearch.minSurface)}}</div>
			<div class="col">{{form_row(formSearch.maxPrice)}}</div>
			<div class="col">{{form_row(formSearch.options)}}</div>
			<div class="col">
				<button class="btn btn-primary">Réchercher</button>
			</div>
		</div>
		{{form_end(formSearch)}}
	</div>
	{{ include ("property/_property.html.twig") }}
	{{ knp_pagination_render(properties) }}
{% endblock %}
